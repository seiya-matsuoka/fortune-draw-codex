# プロジェクト固有の作業方針

このファイルは、`fortune-draw-codex` 固有の Codex 作業方針を定義する。  
グローバル `AGENTS.md` の共通方針に加えて、本プロジェクトでは以下を適用する。

## プロジェクトの目的

- 本プロジェクトの主目的は、Codex を利用した小規模開発の基本的な進め方を体験し、AI エージェントの操作に慣れることとする。
- おみくじアプリ自体の機能量や高度さは重視せず、短い開発サイクルで計画、実装、確認、修正を経験することを優先する。
- 学習目的に対して不要な機能追加、過剰設計、過度な抽象化を行わない。

## 参照するドキュメント

- 要件は `docs/01-requirements.md` を参照する。
- 設計は `docs/02-design.md` を参照する。
- `docs/03-development-plan.md` が存在する場合は、合意済みの実装工程として参照する。
- ドキュメント間に矛盾がある場合は、勝手に解釈して大きな変更を行わず、ユーザーへ確認する。

## 技術上の制約

- HTML / CSS / JavaScript の静的ファイルのみで構成する。
- フレームワーク、外部ライブラリ、パッケージマネージャー、ビルドツールを使用しない。
- 外部 API、データベース、バックエンド、認証機能を使用しない。
- 実装ファイルは原則としてリポジトリ直下の `index.html`、`style.css`、`script.js` とする。
- 要件を満たすために不要なファイルや設定を追加しない。

## 実装方針

- 既存の `AGENTS.md` と `docs/` を確認してから実装する。
- 変更は、その時点で依頼された工程または目的に必要な範囲に限定する。
- 複数工程が定義されている場合は、ユーザーから指定された工程のみ実装し、先の工程へ勝手に進まない。
- HTML は意味の分かりやすい構造を優先する。
- CSS は小規模アプリに適した単純な構成とし、過度な装飾や複雑なアニメーションを追加しない。
- JavaScript は処理の流れを追いやすい単純な実装を優先する。
- コメントは必要な箇所だけに記載し、処理をそのまま読み上げるコメントは避ける。

## 検証

- 実装後は、可能な範囲で内蔵ブラウザまたはローカルブラウザを使用して実際の表示と操作を確認する。
- 少なくとも次を確認する。
  - 初期表示が崩れていないこと。
  - おみくじを引く操作が実行できること。
  - 操作のたびに定義済みの運勢とメッセージのいずれかが表示されること。
  - 主要な操作をキーボードでも実行できること。
  - JavaScript の実行時エラーが発生していないこと。
- 自動テスト環境は新たに導入しない。
- 検証できなかった項目がある場合は、作業完了時に明示する。

## Git

- `git status`、`git diff` などの読み取り専用操作は、変更確認のために使用してよい。
- `git add`、`git commit`、`git push`、branch 操作、Pull Request 操作など Git / GitHub の状態を変更する操作は、ユーザーから明示的に依頼された場合のみ実行する。
- 作業完了時は、ユーザーが差分を確認できるように変更内容を簡潔に報告する。

## `.gitignore` とローカル生成物

- `.gitignore` に記載されていることだけを理由に、すべての対象ファイルを編集禁止とはしない。
- `.env`、認証情報、秘密情報を含む可能性があるローカル設定ファイルは、ユーザーから明示的に依頼されない限り読み取り・編集・削除しない。
- ビルド成果物、ログ、キャッシュ、一時ファイルなどの生成物は手動編集せず、必要な場合は正規のコマンドや処理から生成する。
- 無視対象ファイルに作業上意味のある変更が発生した場合は、`git diff` に表示されない可能性があるため、作業完了時にその事実を報告する。
- 無視対象ファイルを含む一括削除やクリーンアップを独断で実行しない。

## 作業範囲

- 現在の `fortune-draw-codex` ワークスペース外を変更しない。
- 本プロジェクトの目的に不要な外部通信、外部サービス連携、ファイルダウンロードを行わない。
- Web 検索は、仕様確認や技術情報の調査が必要な場合に限り利用し、一次情報を優先する。
